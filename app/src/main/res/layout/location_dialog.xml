<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/shape_alert_dialog"
    android:padding="@dimen/Padding">
    <ImageView
        android:layout_width="@dimen/dialogIconSize"
        android:layout_height="@dimen/dialogIconSize"
        android:src="@drawable/ic_location_menu"
        android:layout_gravity="center_horizontal"
        android:layout_margin="@dimen/dialogMargin"/>
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="@dimen/dialogMargin"
        android:id="@+id/location_layout">
        <Button
            android:layout_width="@dimen/iconSize"
            android:layout_height="@dimen/iconSize"
            android:background="@drawable/ic_search_tv"
            android:layoutDirection="rtl"
            android:id="@+id/location_search_btn"
            android:layout_marginStart="@dimen/Margin"/>
        <EditText
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:singleLine="true"
            android:maxLines="1"
            android:background="@drawable/shape_search_et_location"
            android:hint="@string/location_search_et_hint"
            android:padding="5dp"
            android:maxLength="20"
            android:textSize="@dimen/mediumTextSize"
            android:id="@+id/location_search_field"/>
    </LinearLayout>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/mediumTextSize"
        android:text="@string/location_search_btn"
        android:layout_marginTop="@dimen/Margin"
        android:layout_gravity="center"
        android:background="@android:color/transparent"
        android:id="@+id/my_location_btn"/>
</LinearLayout>